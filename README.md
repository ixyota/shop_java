# 🛒 Online Store — Учебный проект

Добро пожаловать в репозиторий учебного веб-проекта "Online Store", который мы создаём вдвоём.

Цель проекта — изучить Java, научиться работать с GitHub и реализовать полноценное веб-приложение с базой данных. (сако лох)

## 📌 Цели проекта

- Научиться командной разработке
- Получить опыт в создании веб-сайтов
- Использовать GitHub для совместной работы
- Освоить Java в реальном проекте
- Реализовать онлайн-магазин с товарами, корзиной, пользователями и админ-панелью

## 🧰 Технологии проекта

### Backend (Java):
- Java 17+
- Spring Boot
- Spring MVC / Web
- Spring Data JPA / Hibernate
- Maven или Gradle

### Frontend:
- HTML / CSS / JavaScript

### База данных:
- MySQL

## 📂 Структура проекта (будет дополняться)

```
/online-store
 ├── backend/
 │     ├── src/main/java/...
 │     ├── src/main/resources/
 │     └── pom.xml
 ├── frontend/
 │     ├── index.html
 │     ├── css/
 │     └── js/
 ├── docs/
 └── README.md
```

## 🚀 Как запустить проект (когда будет код)

1. Установить JDK 17+
2. Установить СУБД (MySQL)
3. Создать базу данных `online_store`
4. Настроить `application.properties`
5. Запустить Spring Boot:

```bash
mvn spring-boot:run
```

## 🧭 Как правильно пользоваться GitHub (для команды)

### ❗ Главный принцип

**НИКОГДА НЕ РАБОТАЕМ НАПРЯМУЮ В ВЕТКЕ main**

Работаем только в ветках.

### 🔱 Основной рабочий процесс (workflow)

#### 1. Перед началом работы — обновить проект:

```bash
git pull origin main
```

#### 2. Создать свою ветку:

Каждая задача = отдельная ветка.

**Пример:** работа над корзиной

```bash
git checkout -b feature/cart
```

Работа над авторизацией:

```bash
git checkout -b feature/auth
```

#### ✍️ 3. Коммитим изменения

После работы:

```bash
git add .
git commit -m "Добавил контроллер товаров"
```

#### ⬆️ 4. Отправляем свою ветку на GitHub

```bash
git push origin feature/cart
```

#### 🔁 5. Создаём Pull Request (PR)

На GitHub:
- Pull requests → New pull request → выбрать вашу ветку

PR нужен, чтобы:
- второй участник посмотрел изменения
- можно было обсудить
- было видно историю проекта

Только после одобрения — можно мержить в main.

### ⚠ Важные правила, чтобы не ломать проект

- ✔ Делай pull каждый день, прежде чем начинать работу
- ✔ Работай только в своей ветке
- ✔ Не пушь в main
- ✔ Делай понятные названия коммитов
- ✔ Не храни пароли/ключи в коде
- ✔ Всегда делай Pull Request — даже если изменения маленькие

## 📚 Полезные команды Git

**Проверить текущий статус:**

```bash
git status
```

**Переключиться на другую ветку:**

```bash
git checkout имя_ветки
```

**Обновить текущую ветку:**

```bash
git pull
```

**Удалить локальную ветку:**

```bash
git branch -d feature/cart
```

**Удалить ветку на GitHub:**

```bash
git push origin --delete feature/cart
```

## 🗓 Дедлайн

**10 декабря**

До этого времени нужно реализовать минимальный функционал онлайн-магазина.

## 👥 Команда

- **Участник 1: Рахат Орынхан
- **Участник 2: Санжар Аби

